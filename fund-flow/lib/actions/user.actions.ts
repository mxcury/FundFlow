"use server";

export const signIn = async () => {
	try {
	} catch (error) {
		console.error("Error", error);
	}
};

export const signUp = async (userData: SignUpParams) => {
	try {
		// Create user account
	} catch (error) {
		console.error("Error", error);
	}
};
